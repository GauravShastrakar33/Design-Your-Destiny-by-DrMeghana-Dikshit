import { CheckSquare } from "lucide-react";
import PlaceholderPage from "./PlaceholderPage";

export default function ProcessChecklistPage() {
  return (
    <PlaceholderPage
      title="Process Checklist"
      description="Track your daily practices and wellness routines with structured checklists and progress monitoring."
      icon={CheckSquare}
    />
  );
}
